cd /home/sena/Documents/microcapsules
mkdir -p helper
# save the script there as create_microcaps_subscriptions_table.sh

chmod +x helper/create_microcaps_subscriptions_table.sh


vamos a helper

DB_PASSWORD='Integra4for@@$' ./create_microcaps_subscriptions_table.sh





pg hba set up for the new ip network  , We located the ip for the network
sudo systemctl reload postgresql